/// <reference types="react" />
import { MenuProps } from "./types";
declare const Menu: ({ id, children, align, onClose, mountId, closeOnClick, position, xOffset, yOffset, fluid, focusOnOpen, focusOnClose, menuRef, }: MenuProps) => JSX.Element;
export default Menu;
