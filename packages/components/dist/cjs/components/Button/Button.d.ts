/// <reference types="react" />
import type { ButtonProps } from "./types";
declare const Button: ({ id, onClick, disabled, variant, size, text, danger, icon, onResize, htmlTitle, align, type, fluid, wrap, iconSize, }: ButtonProps) => JSX.Element;
export default Button;
